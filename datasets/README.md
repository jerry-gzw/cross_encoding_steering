# Dataset directory

Place the raw files here before running the artifact. See `DATASETS.md` in the
repository root for filenames, source links, and the fields used by each
experiment. Raw datasets are not redistributed by this repository.
